using UnityEngine;
using UnityEngine.UI;

public class PrivacyPolicyOnStart : MonoBehaviour
{
    /*
    public string linkPrivacyPolicy;
    public Button buttonPrivacyPolicy;
    public Button buttonAccept;

    private void Start()
    {
        if (SaveManager.GetKey("PrivacyPolicyOnStart", 0) != 0)
        {
            gameObject.SetActive(false);
        }

        buttonPrivacyPolicy.onClick.AddListener(ButtonPrivacyPolicy);
        buttonAccept.onClick.AddListener(ButtonAccept);
    }

    private void ButtonPrivacyPolicy()
    {
        Application.OpenURL(linkPrivacyPolicy);
    }

    private void ButtonAccept()
    {
        SaveManager.SetKey("PrivacyPolicyOnStart", 1);
        gameObject.SetActive(false);
    }*/
}
